# Implementation Plans

Generated by the `improve` skill on 2026-07-11. Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

All 8 audit findings from the 2026-07-11 session were selected for
planning; finding #7 (`.playwright-mcp/` not gitignored) was folded into
plan 001 as a step rather than kept as its own plan, since both are the
same atomic action (things to handle before the first commit). That leaves
7 plan files for 8 findings.

**Note on drift checks**: no git repository existed in this project when
these plans were written (see plan 001). Every plan's drift-check step
accounts for this — if plan 001 has run by the time you execute a later
plan, use the resulting commit SHA for a real `git diff`; if not, compare
the "Current state" excerpts against the live files by eye.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Initialize git repository (+ gitignore `.playwright-mcp/`) | P1 | S | — | TODO |
| 002  | Add unit tests for `text-similarity.ts`, fix broken `npm test` gate | P1 | S | none (001 recommended first) | TODO |
| 003  | Prevent audio playback and recording from running concurrently | P1 | S | none | TODO |
| 004  | Guarantee recording UI never gets stuck after a speech-recognition error | P2 | S | none (touches same file as 003 — see sequencing note in 004) | TODO |
| 005  | Pin `@tanstack/*` deps to concrete versions instead of `"latest"` | P2 | S | none | TODO |
| 006  | Reconcile `CLAUDE.md`'s atomic-design convention with actual structure | P3 | S | none | TODO |
| 007  | Remove literal `"..."` from practice text in 3 phrases | P3 | S | none | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None of these plans hard-depend on each other to execute correctly. The
  soft ordering above (001 first) exists because:
  - 001 establishes git, which every other plan's drift-check step assumes
    may exist (each plan degrades gracefully to eyeball-diffing if it
    doesn't, but a real `git diff` is safer).
  - 003 and 004 both edit `src/components/PhraseCard/index.tsx`, in
    different, non-overlapping functions. Running 003 before 004 is
    recommended (that's the numbering), but if 004 runs first, re-read the
    file before starting 003 rather than trusting line numbers from either
    plan's "Current state" section.
- 002, 005, 006, 007 are fully independent of everything else and of each
  other — any order, or parallel execution across separate worktrees, is
  fine.

## Findings considered and rejected

None rejected outright — all 8 audit findings from the 2026-07-11 session
were turned into (or folded into) a plan. See individual plans for the
original evidence and reasoning.

## Direction findings (not planned — options for the maintainer)

These were presented alongside the findings table but are not build-it
plans; they're recorded here so a future `/improve` run doesn't
re-surface them as if new:

- **Multi-scene support**: the `Phrase[]` + `/audios/fraseN.mp3` data shape
  generalizes cheaply to a second video/scene. Needs a scene-selector UI
  and a decision on whether scoring persists per scene. Not planned.
- **Session progress / persistence**: `evaluations` state in
  `ListeningSpeakingApp` is lost on every refresh; even `localStorage` of
  best-score-per-phrase would let learners track improvement across
  sessions. Not planned.
